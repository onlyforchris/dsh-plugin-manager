# Changelog

## 0.1.0

- Add a standard `dsh.bundle` package installed through `dsh plugin`.
- Add the native **插件医生** tab under DSH Plugins settings.
- Add read-only Host diagnostics for Node, Profile, Bundle, Loader and Web state.
- Add no-store JSON transport without shell execution or local-path disclosure.
