# UI 标准

本仓库所有界面（包括 dsh-plugin-manager 设置页）必须遵守 `docs/ui-standards.md` 中的通用 UI 标准。

核心红线（摘要）：

- **禁止在面板/卡片左侧或顶部加装饰色条**（如 `border-left: 3px solid` 高亮竖条）。
- **禁止渐变装饰**：按钮、卡片、背景一律纯色；进度条等功能性元素也优先纯色。
- 状态区分只用「底色 + 文字色」小徽章，不加彩色边框。
- 颜色一律使用宿主设计令牌（`--dpm-*` 变量、`Canvas`/`CanvasText`），深浅主题自动适配。

完整规则与验收清单见 `docs/ui-standards.md`。任何 UI 改动提交前必须自查通过。
